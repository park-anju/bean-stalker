# Claude Code project configuration

Project-scoped execution support for Bean Stalker. `CLAUDE.md` remains the constitution/context router; this directory adds deterministic skills, specialized subagents and conservative hooks.
