---
id: REQ-TRACEABILITY
type: requirements-spec
status: approved
version: 1.1
authority: canonical
owner: Project Owner
updated: 2026-09-19
---
# Traceability Matrix

| Requirement | Domain/design | Test target | P0 status |
|---|---|---|---|
| FR-001/002/020 | [[Location Resolution]] | TC-LOC-001..009 | VERIFIED — automatic mocked geolocation success, pending/prompt state, already-available center, granted and denied permission, missing Permissions API fallback, Geolocation denial/unavailable/timeout/unexpected, unsupported API, insecure context, explicit retry success, Strict Mode/rerender deduplication, no-search-without-location, no precise-coordinate display, and raw coordinate-field removal are covered in `useLocation.test.ts`, `LocationSelector.test.tsx`, `DiscoveryPage.test.tsx` and `tests/e2e/location.spec.ts`. |
| FR-003/004 | [[Search Lifecycle]], [[API Contract]] | TC-SEARCH-001..006 | PLANNED — FR-003 automatic resolved-center search is VERIFIED by `DiscoveryPage.test.tsx` and `location.spec.ts` (one automatic search; no Search click; rerender/Strict Mode remains one). Route evidence exists for TC-SEARCH-004/005/006 (`apps/api`, T05); TC-SEARCH-001/002 render a normalized list and show EMPTY, not error, for zero results; **TC-SEARCH-003 VERIFIED** by the deterministic stale-response test. Remaining gap: FR-004 "in live mode" — no live Google Nearby Search has run (T08). |
| FR-005/006 | [[UX Contract]] | TC-UI-001..003 | VERIFIED (mechanism) — T07: TC-UI-001 card↔marker selection sync (unit + `markerLayer.test.ts` + `CafeMap.test.tsx`), TC-UI-002 honest missing-data labels ("No rating data" / "Hours unavailable", never "Closed" for UNKNOWN — `CafeList.test.tsx` + `formatCafe.test.ts` + e2e), TC-UI-003 mobile primary flow (e2e 375px, no overflow, with results). List renders real data; marker rendering is verified against a mocked `google.maps` — live Advanced-Marker render is T08. |
| FR-007 | [[Cafe Discovery Model]] | TC-MAP-001..003 | VERIFIED — T05: TC-MAP-001 (Google fields normalize to `Cafe`), TC-MAP-002 (missing optional fields stay absent/UNKNOWN, never fabricated), TC-MAP-003 (distance delegates to T02's tolerance-tested Haversine helper), all unit-tested against realistic fixtures. |
| FR-008..011 | [[Ranking and Filtering Rules]] | TC-FILTER-001..005 | VERIFIED — T02 unit-tests the helpers (TC-FILTER-001/002/003/005); T09 wires them to UI and verifies the user-observable result: FR-008 distance sort + FR-009 rating sort (incl. the resolved [[Open Questions|OQ-008]] tie-break), FR-010 minimum-rating filter (unrated excluded on a positive threshold, kept at 0), FR-011 Open Now (OPEN only; CLOSED **and** UNKNOWN excluded), combined = intersection, TC-FILTER-004 reset — `filterState.test.ts` + `DiscoveryPage.test.tsx` + e2e `filters.spec.ts`. List and map render the same filtered+sorted set; every control change issues **0** provider requests. |
| FR-012..014 | [[Favorite Cafe Model]] | TC-FAV-001..004 | VERIFIED — T02 unit-tests the pure helpers; **T10** wires them to a versioned `localStorage` boundary and the UI: FR-012 favourite/unfavourite via `FavoriteButton` (`aria-pressed` toggle, `FavoriteButton.test.tsx`), FR-013 persistence across reload (`FavoritesProvider.test.tsx` remount + e2e `favorites.spec.ts` real `page.reload()`), FR-014 view via `/favorites` (`FavoritesPage.test.tsx`). **TC-FAV-001** idempotency (`FavoriteButton.test.tsx` "one record" via two buttons + T02), **TC-FAV-002** remove (page + provider tests), **TC-FAV-003** reload preserves (`favoritesStorage.test.ts` round-trip + e2e), **TC-FAV-004** corrupt store falls back safely (`favoritesStorage.test.ts`: invalid JSON / wrong schema / unsupported `version` / `getItem` throws → empty, never crash). Note: a "favourites only" Discovery filter (FR-014 "filter") is [[Open Questions|OQ-012]]. |
| FR-015/016 | [[Search Lifecycle]], [[Error Catalog]] | TC-ERR-001..005 | VERIFIED — T07: TC-ERR-001 (a provider error renders a distinct `role="alert"`, never the EMPTY copy — `SearchStatePanel.test.tsx` + e2e), TC-ERR-002 (explicit Retry reissues the current search exactly once, no auto-retry — `useCafeSearch.test.tsx` + e2e), TC-ERR-003 (a native `AbortError` from a superseded search is re-thrown, not shown as a failure — `apiClient.test.ts`). Explicit loading/empty/error states rendered by `SearchStatePanel`. **H05 extends this:** 429 `RATE_LIMITED` and 503 `PROVIDER_CAPACITY_EXHAUSTED` are distinct codes with distinct bounded copy ("searching too quickly" vs "temporarily unavailable"), each retryable, neither auto-retrying, neither leaking IP/counter/pricing detail — `errorCopy.test.ts`, `SearchStatePanel.test.tsx`, `apiClient.test.ts`, e2e `capacity.spec.ts` (resolved location retained, favourites route still reachable, no crash/loop). |
| FR-017 | [[Cafe Discovery Model]] | TC-UI-004 | VERIFIED — T07: the "Open in Google Maps" link renders only when `googleMapsUri` is present (`CafeList.test.tsx` + `search.spec.ts`); the unrated fixture cafe with no URI shows no link. |
| FR-018 | [[Business Rules]], [[API Contract]] | TC-API-001..004 | VERIFIED — T05: all four route-tested (invalid params/missing field/unknown field rejected before the provider is ever called; malformed body still returns the stable envelope). |
| FR-019 | [[API Contract]] | TC-API-005 | VERIFIED — T05: `/health` returns `{"status":"ok"}` with no secret exposure (existing test + manual curl + code inspection of the route). |
| NFR-001/002 | [[Threat Model]], [[Privacy Boundaries]] | TC-SEC-001..004 | PLANNED — evidence exists for TC-SEC-001 (server key absent from browser bundle, T01/T04/T05 regression-checked), TC-SEC-002 (provider errors never echo Google's raw body or the API key, T05 unit test), TC-SEC-003 (malformed/oversized input rejected safely, T05 route tests); **TC-SEC-004 VERIFIED** (T10): `FavoriteStore` stores only `{ version, cafes: [{ placeId, savedAt, snapshot: Cafe }] }` — no `SearchCenter`, no user coordinates, no request IDs, no provider payload/errors, no credentials; `grep` of `apps/web/src/favorites/` finds no `SearchCenter`/`requestId`/`GOOGLE` tokens, and `favoritesStorage.test.ts` asserts the round-tripped store equals the input. TC-SEC-001 re-checked after T09/T10: `grep` of `apps/web/dist` still finds no server key / `places.googleapis` / `X-Goog-Api-Key`; the browser Maps key and `DEMO_MAP_ID` are present as expected. T07–T10 persist/log no user coordinates client-side (favourites hold public cafe coordinates only, per [[Privacy Boundaries]]). **H02 adds server-side logging evidence** (`apps/api/src/test/logging.test.ts`): with a capturing log stream, a search sent with conspicuous coordinates `1.234567 / 110.987654` and `remoteAddress 203.0.113.42` produces log lines containing none of those literals, no `remoteAddress` key, and no request body — while `method` / path / `statusCode` remain; a thrown error carrying attached `rawProviderResponse` / `apiKey` sentinels logs neither; a manual `app.log.info({ apiKey: 'THIS_MUST_NEVER_APPEAR_IN_LOGS', body: {...} })` is redacted. **H06/H07 ([[ADR-009 API Security Posture]]) add:** `CAFE_PROVIDER=live` fails env validation without `GOOGLE_PLACES_SERVER_KEY` / `PROVIDER_MONTHLY_REQUEST_LIMIT` and fixture mode needs neither (`env.test.ts`); a validation error never echoes an offending value; `HttpOriginSchema` rejects a non-origin `WEB_ORIGIN`/`VITE_API_BASE_URL` (`packages/contracts/env.test.ts` + both app env tests); `GOOGLE_PLACES_TIMEOUT_MS` is bounded 100–30000. `apps/web`'s `build` runs `scripts/check-frontend-dist-secrets.mjs` — the build fails if `dist/` contains `GOOGLE_PLACES_SERVER_KEY` / `X-Goog-Api-Key` / `places.googleapis.com` (proven to detect via a forced sentinel); git history verified clean of real key patterns. `security.test.ts`: strict single-origin CORS never reflects `http://evil.example`; `nosniff` / `no-referrer` / `X-Frame-Options: DENY` on every response incl. errors; a 64 KiB body → 4xx canonical `VALIDATION_ERROR` with `searchNearby` uncalled and `usage.used === 0` and no limit value leaked; an unknown route / `PUT /api/v1/cafes/search` → canonical `NOT_FOUND` 404 with no route pattern; an unexpected error with `/home/user/private/...` + `SECRET_INTERNAL_DETAIL_123` → generic 500 envelope, sentinels absent; `/health` returns only `{status:'ok'}`. |
| NFR-003/009 (RM0) | [[Performance Test Plan]], [[API Cost Guardrail Runbook]], [[ADR-007 Cost-Safe Search Orchestration]] | PERF-001..003 | PLANNED (PERF-*) — but the RM0 frontend request-discipline half is VERIFIED (T07): tests prove one request per committed centre and **zero** provider requests from rerender / window focus / network reconnect / remount / map pan-zoom / marker or card selection / error auto-retry, plus cache reuse for an identical fresh search (`useCafeSearch.test.tsx`, `search.spec.ts`). **T09 extends the verified half:** local rating filter, Open Now toggle, sort change and Reset each issue **0** `POST /api/v1/cafes/search` requests, and a filtered-empty result never triggers an expansion/refetch — `DiscoveryPage.test.tsx` asserts `searchCafes` call count stays 1, e2e `filters.spec.ts` asserts a live request counter stays 1, and no filter/sort field appears anywhere under `apps/web/src/search/`. **H03/H04 add the server half** ([[ADR-008 Metered Provider Cost Controls]]): `rateLimiter.test.ts` + `providerUsageGuard.test.ts` + `test/cafeSearchGuards.test.ts` prove a per-client 429 after the window limit (provider not called, no usage consumed), a global 503 at the monthly cap (provider not called), consume-before-dispatch with no refund on provider failure, no consumption for invalid/rate-limited requests, UTC-month rollover reset (injected clock), and that concurrent `Promise.all` `tryConsume` never exceeds the limit; `/health` is never rate-limited. Google-side quotas / budget alerts / a durable guard remain deferred — [[Known Blockers|BLK-003]] / [[Known Blockers|BLK-004]]. |
| NFR-004/005 | [[UX Contract]] | TC-A11Y-001..003 | PLANNED — TC-A11Y-001 (keyboard) has shell/nav evidence (T03), T07 evidence for keyboard-operable cafe cards, and T09 evidence for native labelled filter controls. **TC-A11Y-003 VERIFIED**: list and map consume the same filtered+sorted `Cafe[]`, and e2e proves the list survives a blocked Maps script. NFR-005 responsive: e2e verifies results/filter/favourite/location-error states without horizontal overflow. **TC-A11Y-002 VERIFIED**: favourite state uses a native sibling `<button aria-pressed>` and `/favorites` uses a semantic list. H08 established the keyboard/focus/contrast/target-size/axe baseline. **H11 supersedes the old coordinate-form evidence:** the automatic location/search flow is keyboard-safe; progress uses labelled `role="status"`; failures use `role="alert"`; recoverable failures expose **Try location again**; coordinate inputs are absent; Playwright axe scans the pending-location and location-error states with 0 violations. Automated scans supplement manual review and are not a WCAG-conformance claim. |
| NFR-006..008 | [[Test Strategy]], [[Observability Runbook]] | TC-ERR/OPS | PLANNED — T07 adds NFR-006 (reliability) evidence: a Maps-script failure never crashes the Discovery page (`CafeMap.test.tsx` + e2e), and a cached/EMPTY result is never presented as a fresh successful provider response (`SearchStatePanel`, `useCafeSearch`). T09 adds: a filtered-empty result is shown as "No cafes match your current filters" — never conflated with the API-empty "No cafes were found near this location" (`DiscoveryPage.test.tsx` + e2e). **H05 adds NFR-006 evidence:** a 429 or 503 capacity response degrades gracefully — no crash, no retry loop, resolved location kept, favourites route still works (`capacity.spec.ts`). **H02 gives NFR-008 concrete evidence** (see the NFR-001/002 row): the logging policy is code-enforced in `apps/api/src/logging.ts` and regression-tested. |


## Evidence rule

`PLANNED` may become `VERIFIED` only when the exact test/build/manual evidence is recorded in [[Implementation Handoffs]]. Documentation existence alone is not implementation evidence.
